# CHANGELOG

## [1.0.0] - 2026-01-14
### Added
- Initial release of Canvas-integrated Agent system.
- Core `CanvasEngine` for parsing `.canvas` files.
- `StructureManager` for automated directory/file generation.
- `ReverseGenerator` for scanning existing projects and creating visual architecture.
- CDD (Canvas-Driven Development) protocol in `AGENTS.md`.
- Visual architecture map in `architecture.canvas`.
